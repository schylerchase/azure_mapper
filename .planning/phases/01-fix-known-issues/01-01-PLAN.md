---
phase: 01-fix-known-issues
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app-core.js
  - src/modules/demo-data.js
autonomous: true
requirements:
  - R5.1
  - R5.2

must_haves:
  truths:
    - "Chrome file upload via Import Folder button works without stale dialogs"
    - "Ephemeral file input elements are cleaned up after use"
    - "Demo data shows Azure subscription labels instead of AWS account IDs"
  artifacts:
    - path: "src/app-core.js"
      provides: "Fixed file upload with cleanup and Azure demo labels"
      contains: "inp.remove()"
    - path: "src/modules/demo-data.js"
      provides: "Azure-native demo data generator"
  key_links:
    - from: "src/app-core.js"
      to: "src/modules/demo-data.js"
      via: "generateDemo() import and account label references"
      pattern: "addAccountContext"
---

<objective>
Fix Chrome file upload stale dialog bug and update demo data account labels from AWS format to Azure-native format.

Purpose: Resolve two quick bugs from M1 migration -- the Chrome file chooser creates ephemeral input elements that linger in the DOM (causing stale dialogs), and the demo mode still displays AWS-style account labels instead of Azure subscription names.
Output: Working Chrome file upload, Azure-native demo labels
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Chrome file upload stale file chooser</name>
  <files>src/app-core.js</files>
  <read_first>src/app-core.js (lines 11573-11688 for browser folder import, lines 20264-20282 for govRulesImport, lines 23248-23274 for fileInput change handler)</read_first>
  <action>
Fix ephemeral file input elements that linger in the DOM after use in two locations within src/app-core.js:

1. **_folderFallback function (around line 11578):** After `const inp=document.createElement('input')`, add cleanup after the change handler completes. After the `Promise.all(pending).then(...)` block resolves (around line 11627), add `inp.remove();`. Also add `inp.remove();` in the case where `!inp.files||!inp.files.length` returns early (line 11582). The pattern:
   ```
   inp.addEventListener('change',()=>{
     if(!inp.files||!inp.files.length){inp.remove();return}
     // ... existing logic ...
     Promise.all(pending).then(()=>{
       // ... existing importFolder logic ...
       inp.remove();
     });
   });
   ```
   Also add a one-time abort listener to clean up if user cancels:
   ```
   inp.addEventListener('cancel',()=>inp.remove());
   ```

2. **govRulesImport handler (around line 20264):** Same pattern -- after `var inp=document.createElement('input')`, add cleanup. After `reader.readAsText(this.files[0])` completes and after the early return on `!this.files[0]`, add `inp.remove()` in both paths. Add cancel listener: `inp.addEventListener('cancel',()=>inp.remove());`

3. **showDirectoryPicker call (line 11631):** This is already correct -- it uses the browser's native directory picker API which doesn't create DOM elements. No change needed.

4. **Persistent fileInput (line 23001/23272):** Already correct -- it's a permanent DOM element with `this.value=''` reset. No change needed.
  </action>
  <verify>
    <automated>grep -n "inp.remove()" src/app-core.js | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "inp.remove()" src/app-core.js returns at least 4 matches (2 in _folderFallback, 2+ in govRulesImport)
    - grep "cancel.*inp.remove\|inp.remove.*cancel" src/app-core.js returns at least 2 matches (cancel listeners)
    - The _folderFallback function still creates input with type='file', webkitdirectory, and multiple attributes
    - The fileInput permanent element (id="fileInput") is NOT modified
  </acceptance_criteria>
  <done>Ephemeral file input elements are removed from DOM after use or cancellation in both _folderFallback and govRulesImport handlers</done>
</task>

<task type="auto">
  <name>Task 2: Update demo data to Azure-native subscription labels</name>
  <files>src/app-core.js</files>
  <read_first>src/app-core.js (lines 23292-23390 for loadDemo click handler, specifically lines 23373-23374 for addAccountContext calls)</read_first>
  <action>
Update the demo data account labels in the loadDemo click handler (src/app-core.js around line 23373-23374) from AWS-style to Azure subscription format.

Change these two lines:
```
addAccountContext({textareas:buildAcctTextareas(acct1Vpcs),accountLabel:'prod-account (111122223333)'},'prod-account (111122223333)');
addAccountContext({textareas:buildAcctTextareas(acct2Vpcs),accountLabel:'security-ops (444455556666)'},'security-ops (444455556666)');
```

To use Azure subscription name format (matching the SUB_ID and LH_SUB_ID from demo-data.js):
```
addAccountContext({textareas:buildAcctTextareas(acct1Vpcs),accountLabel:'Production (a1b2c3d4)'},'Production (a1b2c3d4)');
addAccountContext({textareas:buildAcctTextareas(acct2Vpcs),accountLabel:'Security-Ops (c9d8e7f6)'},'Security-Ops (c9d8e7f6)');
```

The format uses the subscription display name + first 8 chars of the subscription GUID in parentheses, which matches Azure Portal convention. The IDs a1b2c3d4 and c9d8e7f6 correspond to the first 8 chars of SUB_ID='a1b2c3d4-e5f6-7890-abcd-ef1234567890' and LH_SUB_ID='c9d8e7f6-5a4b-3c2d-1e0f-lighthouse12345' defined in demo-data.js.
  </action>
  <verify>
    <automated>grep -n "111122223333\|444455556666\|prod-account\|security-ops" src/app-core.js | head -5; echo "---"; grep -n "Production (a1b2c3d4)\|Security-Ops (c9d8e7f6)" src/app-core.js | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "111122223333" src/app-core.js returns 0 matches
    - grep "444455556666" src/app-core.js returns 0 matches
    - grep "prod-account" src/app-core.js returns 0 matches (the old AWS-style label is gone)
    - grep "Production (a1b2c3d4)" src/app-core.js returns exactly 2 matches (label param + display name param)
    - grep "Security-Ops (c9d8e7f6)" src/app-core.js returns exactly 2 matches
  </acceptance_criteria>
  <done>Demo data shows Azure subscription labels "Production (a1b2c3d4)" and "Security-Ops (c9d8e7f6)" instead of AWS-style "prod-account (111122223333)"</done>
</task>

</tasks>

<verification>
- All ephemeral file input elements are cleaned up (grep confirms inp.remove() calls)
- Demo data labels use Azure subscription format (no AWS account IDs remain)
- Persistent fileInput element is untouched
</verification>

<success_criteria>
- Chrome file upload creates and destroys ephemeral inputs cleanly
- Demo mode displays Azure subscription names with short GUIDs
- No AWS-style account labels (111122223333, 444455556666) remain in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-known-issues/01-01-SUMMARY.md`
</output>
