---
phase: 05-release-preparation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - package.json
autonomous: false
requirements: [R4.3, R4.4, R4.6]

must_haves:
  truths:
    - "Electron builder files list includes all 4 JS bundles from dist/"
    - "Local macOS build produces DMG and ZIP artifacts"
    - "Auto-updater configuration points to GitHub releases with correct repo"
    - "Demo data renders Azure-native subscription labels, not AWS numeric IDs"
  artifacts:
    - path: "package.json"
      provides: "electron-builder files list with lazy bundles"
      contains: "dist/demo-data.bundle.js"
    - path: "dist/mac-arm64/"
      provides: "Local macOS build output"
  key_links:
    - from: "package.json build.files"
      to: "dist/demo-data.bundle.js"
      via: "electron-builder includes list"
      pattern: "demo-data\\.bundle\\.js"
    - from: "package.json build.publish"
      to: "GitHub Releases"
      via: "electron-updater provider config"
      pattern: '"provider": "github"'
---

<objective>
Verify Electron packaging includes lazy bundles, confirm auto-update config, and validate R4.6 demo data.

Purpose: Ensure the Electron build includes all required assets (including Phase 4 lazy bundles that were never added to the files list), auto-update will work against GitHub Releases for v2.0.0, and demo data is confirmed clean of AWS artifacts.

Output: Updated electron-builder files list, verified local macOS build, confirmed auto-update config and demo data labels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-release-preparation/05-RESEARCH.md
@.planning/phases/05-release-preparation/05-01-SUMMARY.md

<interfaces>
From package.json build.files (current -- missing lazy bundles):
```json
"files": [
  "index.html", "main.js", "main-utils.js", "preload.js",
  "export-azure-data.sh", "routing.js", "libs/**",
  "dist/app.bundle.js", "dist/app-core.js",
  "src/styles/**", "src/data/**", "logo.png", "icon.png"
]
```

From package.json build.publish (auto-update target):
```json
"publish": {
  "provider": "github",
  "owner": "schylerchase",
  "repo": "Azure-Mapper"
}
```

From main.js (auto-updater, near end of file):
```javascript
// electron-updater checkForUpdates wired with 5-second delay after app ready
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lazy bundles to electron-builder files list and verify build</name>
  <files>package.json</files>
  <read_first>
    - package.json lines 30-46 (build.files array -- currently lists dist/app.bundle.js and dist/app-core.js but NOT dist/demo-data.bundle.js or dist/iac-generator.bundle.js)
  </read_first>
  <action>
    **R4.3 - Fix electron-builder files list:**
    1. In package.json, in the `build.files` array, after `"dist/app-core.js"` (currently last dist entry), add two new entries:
       ```
       "dist/demo-data.bundle.js",
       "dist/iac-generator.bundle.js"
       ```
       These lazy bundles were introduced in Phase 4 but never added to the electron-builder files list. Without them, the packaged Electron app will fail to load demo data or generate IaC exports.

    **R4.3 - Verify local macOS build:**
    2. Run `npm run bundle:prod` to ensure all 4 bundles are freshly built.
    3. Run `npx electron-builder --mac --publish never` to build locally without publishing.
       - Use `--publish never` to prevent accidentally pushing to GitHub Releases.
       - Expected output: `dist/mac-arm64/` directory with the .app, plus `dist/Azure Network Mapper-*.dmg` and `dist/Azure Network Mapper-*.zip`.
    4. Verify the built .app contains the lazy bundles:
       ```bash
       ls "dist/mac-arm64/Azure Network Mapper.app/Contents/Resources/app/dist/"
       ```
       Must list all 4 JS files: app.bundle.js, app-core.js, demo-data.bundle.js, iac-generator.bundle.js.

    **R4.4 - Verify auto-update configuration:**
    5. Confirm package.json `build.publish` has `"provider": "github"` with correct owner/repo. No changes needed if already set to `schylerchase/Azure-Mapper`.
    6. Verify main.js contains the `checkForUpdates` call from electron-updater. No changes needed -- just confirm it exists and references no hardcoded version.

    **R4.6 - Verify demo data is Azure-native:**
    7. Run `grep -rn "[0-9]\{12\}" src/data/` to check for any 12-digit AWS-style account IDs in demo data files. Expected: no matches (Phase 1 already converted to Azure subscription UUIDs).
    8. If any AWS-style IDs are found, replace them. (Research indicates none exist -- this is a safety check.)
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const f=p.build.files; if(!f.includes('dist/demo-data.bundle.js')) throw new Error('missing demo-data'); if(!f.includes('dist/iac-generator.bundle.js')) throw new Error('missing iac-generator'); console.log('PASS: electron-builder files list includes lazy bundles')" && grep -cE "[0-9]{12}" src/data/* 2>/dev/null | grep -v ":0$" | wc -l | xargs test 0 -eq && echo "PASS: no AWS-style IDs in demo data"</automated>
  </verify>
  <acceptance_criteria>
    - package.json build.files includes "dist/demo-data.bundle.js" and "dist/iac-generator.bundle.js"
    - Local macOS build completes without errors
    - Built .app contains all 4 dist/*.js bundles
    - build.publish provider is "github" with correct owner/repo
    - main.js has electron-updater checkForUpdates wired (no hardcoded version)
    - No 12-digit AWS-style account IDs in src/data/
  </acceptance_criteria>
  <done>Electron packaging includes all lazy bundles, auto-update config verified, demo data confirmed Azure-native</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Electron build and demo data visually</name>
  <files>dist/mac-arm64/</files>
  <action>
    This is a human verification checkpoint. Claude has already built the Electron app in Task 1.
    The user needs to visually confirm the packaged app works correctly:
    - About dialog shows dynamic version v2.0.0
    - Demo data loads and renders with Azure-native labels
    - IaC generator lazy bundle loads in the packaged app
  </action>
  <what-built>
    Electron macOS build with all 4 JS bundles, version 2.0.0 in About dialog, and Azure-native demo data.
  </what-built>
  <how-to-verify>
    1. Open the built app: `open "dist/mac-arm64/Azure Network Mapper.app"`
    2. Check About dialog: Menu -> Help -> About Azure Network Mapper
       - Expected: Shows "Azure Network Mapper v2.0.0"
    3. Load demo data: Click the demo data button in the app
       - Expected: Map renders with Azure subscription labels (UUID format), NOT 12-digit AWS account IDs
    4. Check IaC generator: Open the IaC generator from the dock
       - Expected: Loads without errors (lazy bundle works in packaged app)
    5. Close the app
  </how-to-verify>
  <verify>Human confirms all 5 checks pass</verify>
  <done>User has visually confirmed the packaged Electron app works correctly with v2.0.0, demo data, and lazy bundles</done>
  <resume-signal>Type "approved" if all checks pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `node -e "const f=require('./package.json').build.files; console.log(f.filter(x=>x.includes('dist/')))"` lists all 4 dist entries
2. `ls dist/mac-arm64/` shows the .app directory
3. `grep -c "checkForUpdates" main.js` returns >= 1
4. `grep -rn "[0-9]\{12\}" src/data/ | grep -v ".azuremap"` returns empty (no AWS IDs)
</verification>

<success_criteria>
- electron-builder files list includes all 4 JS bundles from dist/
- Local macOS build succeeds and produces DMG + ZIP artifacts
- About dialog shows "v2.0.0" (dynamic from package.json)
- Auto-updater config points to correct GitHub repo
- Demo data uses Azure subscription UUIDs, no AWS numeric account IDs
- Human verification confirms app launches, renders demo, and shows correct version
</success_criteria>

<output>
After completion, create `.planning/phases/05-release-preparation/05-02-SUMMARY.md`
</output>
