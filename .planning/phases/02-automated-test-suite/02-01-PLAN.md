---
phase: 02-automated-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/normalization.js
  - src/app-core.js
autonomous: true
requirements:
  - R1.1
  - R1.3
  - R1.4
  - R1.5

must_haves:
  truths:
    - "_normalizeAzureResources, _normTags, matchFile, fileMap, and _friendlyFolderLabel are importable from src/modules/normalization.js"
    - "app-core.js imports these functions from normalization.js instead of defining them locally"
    - "npm run bundle succeeds with zero errors after extraction"
    - "Existing test suite still passes (node --test tests/unit/*.test.mjs)"
  artifacts:
    - path: "src/modules/normalization.js"
      provides: "Extracted normalization functions"
      exports: ["_normalizeAzureResources", "_normTags", "matchFile", "fileMap", "_friendlyFolderLabel"]
    - path: "src/app-core.js"
      provides: "Updated to import from normalization.js"
      contains: "import.*normalization"
  key_links:
    - from: "src/app-core.js"
      to: "src/modules/normalization.js"
      via: "ESM import"
      pattern: "import.*from.*modules/normalization"
    - from: "src/modules/normalization.js"
      to: "tests/unit/ (future)"
      via: "ESM export"
      pattern: "export.*_normalizeAzureResources"
---

<objective>
Extract untestable private functions from the app-core.js IIFE into a new src/modules/normalization.js module so they become importable for unit testing.

Purpose: R1.1, R1.3, R1.4, R1.5 all require functions that are currently trapped inside app-core.js with no exports. This extraction is the prerequisite for all normalization unit tests.

Output: src/modules/normalization.js with 5 exported functions; app-core.js updated to import them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-test-suite/02-RESEARCH.md
@src/app-core.js (lines 8783-8832: _normalizeAzureResources + _normTags)
@src/app-core.js (lines 11304-11317: _friendlyFolderLabel)
@src/app-core.js (lines 22860-23003: fileMap + matchFile)
@src/modules/utils.js (lines 57-71: ext function — used by normalization)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/modules/normalization.js with extracted functions</name>
  <files>src/modules/normalization.js</files>
  <read_first>
    - src/app-core.js (lines 8783-8832 for _normalizeAzureResources and _normTags)
    - src/app-core.js (lines 11304-11317 for _friendlyFolderLabel)
    - src/app-core.js (lines 22860-23003 for fileMap and matchFile)
    - src/modules/diff-engine.js (end of file — example of export {} block pattern)
  </read_first>
  <action>
Create a new file `src/modules/normalization.js` containing these five items extracted verbatim from app-core.js:

1. **`_normTags(r)`** — Promote from nested function inside _normalizeAzureResources to a standalone module-level function. Copy verbatim from lines 8787-8791. It is a pure function with no closure dependencies.

2. **`_normalizeAzureResources(d)`** — Copy from lines 8785-8832. Change the nested `_normTags` call to reference the now-module-level `_normTags` (no code change needed since the function name stays the same; just removing the nested definition).

3. **`fileMap`** — Copy the const array from lines 22860-22930. This is a 70-entry array of `{id, patterns}` objects.

4. **`matchFile(fname, content)`** — Copy from lines 22932-23003. It references `fileMap` which will be in the same module.

5. **`_friendlyFolderLabel(folderName)`** — Copy from lines 11304-11317. Pure function, no dependencies.

End the file with a single export block following the project convention:
```javascript
export { _normalizeAzureResources, _normTags, matchFile, fileMap, _friendlyFolderLabel };
```

Do NOT add DOM guards (these functions have zero DOM references). Do NOT reformat/refactor the function bodies — extract verbatim to ensure behavioral equivalence.
  </action>
  <verify>
    <automated>node -e "import('./src/modules/normalization.js').then(m => { const fns = ['_normalizeAzureResources','_normTags','matchFile','fileMap','_friendlyFolderLabel']; const missing = fns.filter(f => !m[f]); if(missing.length) { console.error('Missing exports:', missing); process.exit(1); } console.log('All 5 exports present'); })"</automated>
  </verify>
  <acceptance_criteria>
    - src/modules/normalization.js exists
    - grep confirms all 5 exports: `grep -c 'export.*_normalizeAzureResources.*_normTags.*matchFile.*fileMap.*_friendlyFolderLabel' src/modules/normalization.js` returns 1
    - `_normTags` is a module-level function (not nested inside `_normalizeAzureResources`)
    - `fileMap` array has 70 entries: `node -e "import('./src/modules/normalization.js').then(m=>console.log(m.fileMap.length))"` prints 70
  </acceptance_criteria>
  <done>normalization.js exists with all 5 functions/constants exported and individually importable from Node.js</done>
</task>

<task type="auto">
  <name>Task 2: Update app-core.js to import from normalization.js and verify build</name>
  <files>src/app-core.js</files>
  <read_first>
    - src/app-core.js (lines 1-30 for existing import pattern at top of file)
    - src/app-core.js (lines 8783-8832 for _normalizeAzureResources block to remove)
    - src/app-core.js (lines 11304-11317 for _friendlyFolderLabel block to remove)
    - src/app-core.js (lines 22860-23003 for fileMap + matchFile blocks to remove)
    - src/modules/normalization.js (just created — verify exports)
  </read_first>
  <action>
Modify `src/app-core.js` in three steps:

**Step 1: Add import statement.** Near the top of app-core.js where other module imports exist, add:
```javascript
import { _normalizeAzureResources, matchFile, _friendlyFolderLabel } from './modules/normalization.js';
```
Note: `_normTags` and `fileMap` are NOT imported into app-core.js because they are only used internally by the exported functions. Only import what app-core.js calls directly.

Verify which of these three functions app-core.js actually calls directly by searching for their usage:
- `_normalizeAzureResources` — called in `_renderMapInner` and `_buildRlCtxFromData`
- `matchFile` — called in the file upload handler around line 23004+
- `_friendlyFolderLabel` — called in `importFolder` around line 11321

**Step 2: Remove the original function definitions** from app-core.js:
- Delete lines containing `_normalizeAzureResources` function definition (lines ~8785-8832)
- Delete lines containing `_friendlyFolderLabel` function definition (lines ~11304-11317)
- Delete `fileMap` const array (lines ~22860-22930)
- Delete `matchFile` function (lines ~22932-23003)

Keep the comment `// Shared Azure normalization:...` above the old definition as a breadcrumb pointing to normalization.js:
```javascript
// Shared Azure normalization — extracted to src/modules/normalization.js
```

**Step 3: Verify build.** Run `npm run bundle` and confirm:
- Exit code 0
- dist/app-core.js is produced
- dist/app.bundle.js is produced
- No errors in output

Then run `node --test tests/unit/*.test.mjs` to confirm all 232 existing tests still pass.
  </action>
  <verify>
    <automated>cd /Users/schylerryan/Desktop/Github/Azure-Mapper && npm run bundle 2>&1 | tail -5 && echo "---" && node --test tests/unit/*.test.mjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import.*from.*modules/normalization" src/app-core.js` returns 1
    - `grep -c "function _normalizeAzureResources" src/app-core.js` returns 0 (definition removed)
    - `grep -c "function matchFile" src/app-core.js` returns 0 (definition removed)
    - `grep -c "function _friendlyFolderLabel" src/app-core.js` returns 0 (definition removed)
    - `npm run bundle` exits 0
    - `node --test tests/unit/*.test.mjs` shows 232 passing, 0 failing
  </acceptance_criteria>
  <done>app-core.js imports from normalization.js, all original definitions removed, build succeeds, existing tests pass</done>
</task>

</tasks>

<verification>
1. `node -e "import('./src/modules/normalization.js').then(m => console.log(Object.keys(m).sort().join(', ')))"` outputs: `_friendlyFolderLabel, _normTags, _normalizeAzureResources, fileMap, matchFile`
2. `npm run bundle` exits 0 with no errors
3. `node --test tests/unit/*.test.mjs` — all 232 tests pass
4. `npm run test:jest` — all 95 Jest tests pass
</verification>

<success_criteria>
- src/modules/normalization.js exists with 5 exports
- app-core.js has zero local definitions of the extracted functions
- Build pipeline (esbuild) produces dist/ output without errors
- Full existing test suite passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-automated-test-suite/02-01-SUMMARY.md`
</output>
