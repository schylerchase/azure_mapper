---
phase: 02-automated-test-suite
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/unit/normalization.test.mjs
  - tests/unit/integration.test.mjs
  - tests/unit/rlctx-regression.test.mjs
  - tests/unit/utils.test.mjs
  - package.json
autonomous: true
requirements:
  - R1.1
  - R1.2
  - R1.3
  - R1.4
  - R1.5
  - R1.6
  - R1.7
  - R1.8

must_haves:
  truths:
    - "_normalizeAzureResources maps Azure VNet properties to internal format (VpcId, CidrBlock)"
    - "_normalizeAzureResources maps Azure subnet properties to internal format (SubnetId, VpcId, CidrBlock)"
    - "_normalizeAzureResources maps Azure NSG security rules to IpPermissions format"
    - "_normTags converts Azure tag objects to [{Key,Value}] arrays"
    - "_normTags falls back to resource.name when no tags present"
    - "ext() returns flat Azure CLI arrays as-is when no keys match but objects have .id/.name/.type"
    - "ext() handles wrapped {value:[...]} objects"
    - "matchFile maps known filenames (vnets.json, subnets.json, nsgs.json) to correct input IDs"
    - "matchFile falls back to content-based detection for unknown filenames"
    - "_friendlyFolderLabel extracts display name from azure-export-* folders"
    - "_friendlyFolderLabel truncates GUID subscription IDs"
    - "Loading a real Azure export folder and normalizing produces VNets with VpcId and CidrBlock set"
    - "No src/modules/*.js file uses stale AWS-internal _rlCtx key names"
    - "Dashboard module files export expected rendering functions (DOM-less structural check for R1.7)"
  artifacts:
    - path: "tests/unit/normalization.test.mjs"
      provides: "Unit tests for R1.1, R1.3, R1.4, R1.5"
      min_lines: 150
    - path: "tests/unit/integration.test.mjs"
      provides: "Integration test for R1.6"
      min_lines: 40
    - path: "tests/unit/rlctx-regression.test.mjs"
      provides: "Regression test for R1.8 + structural check for R1.7"
      min_lines: 40
    - path: "tests/unit/utils.test.mjs"
      provides: "Extended ext() tests for R1.2"
      contains: "flat Azure CLI"
  key_links:
    - from: "tests/unit/normalization.test.mjs"
      to: "src/modules/normalization.js"
      via: "ESM import"
      pattern: "import.*from.*normalization"
    - from: "tests/unit/integration.test.mjs"
      to: "azure-export-00b800a2-*/vnets.json"
      via: "fs.readFileSync"
      pattern: "readFileSync.*azure-export"
    - from: "tests/unit/rlctx-regression.test.mjs"
      to: "src/modules/*.js"
      via: "fs.readdirSync + readFileSync"
      pattern: "readdirSync.*modules"
---

<objective>
Write the complete automated test suite covering all R1.x requirements: unit tests for normalization functions, ext() edge cases, integration test with real export data, _rlCtx regression test, and a structural R1.7 check.

Purpose: Establish test coverage for the core data pipeline so future changes to normalization, file matching, or _rlCtx keys are caught automatically.

Output: 3 new test files + extended utils.test.mjs, all passing under `node --test tests/unit/*.test.mjs`. Updated package.json test script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-test-suite/02-RESEARCH.md
@.planning/phases/02-automated-test-suite/02-01-SUMMARY.md
@src/modules/normalization.js (created in Plan 01)
@src/modules/utils.js (ext function at lines 57-71)
@tests/unit/utils.test.mjs (existing test patterns)

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 extraction -->

From src/modules/normalization.js (created in 02-01):
```javascript
export { _normalizeAzureResources, _normTags, matchFile, fileMap, _friendlyFolderLabel };
```

_normalizeAzureResources(d) — accepts object with keys: vpcs, subnets, rts, sgs, enis, nats, vpces, instances, albs, tgs, peerings, vpns, volumes, snapshots, s3bk, zones, wafAcls, rdsInstances, ecsServices, lambdaFns, ecacheClusters, redshiftClusters, tgwAttachments, cfDistributions. Mutates in place. No return value.

_normTags(r) — accepts single resource object. Mutates r.Tags in place. No return value.

matchFile(fname, content) — returns string input ID (e.g., 'in_vnets') or null.

fileMap — array of 70 {id: string, patterns: string[]} objects.

_friendlyFolderLabel(folderName) — returns string or null.

From src/modules/utils.js:
```javascript
export function ext(r, keys) { ... }  // lines 57-71
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for normalization functions (R1.1, R1.3, R1.4, R1.5) and ext() edge cases (R1.2)</name>
  <files>tests/unit/normalization.test.mjs, tests/unit/utils.test.mjs</files>
  <read_first>
    - src/modules/normalization.js (the extraction from Plan 01 — read entire file for exact function signatures)
    - src/modules/utils.js (lines 57-71 for ext() implementation)
    - tests/unit/utils.test.mjs (existing ext() tests to understand coverage gaps)
    - .planning/phases/02-automated-test-suite/02-RESEARCH.md (code examples section for expected I/O)
  </read_first>
  <behavior>
**normalization.test.mjs — R1.1: _normalizeAzureResources**
- Test: VNet normalization — input `{vpcs: [{id: '/subscriptions/x/providers/Microsoft.Network/virtualNetworks/hub', addressSpace: {addressPrefixes: ['10.0.0.0/16']}, name: 'hub'}]}`, assert `vpcs[0].VpcId === '/subscriptions/x/providers/Microsoft.Network/virtualNetworks/hub'` and `vpcs[0].CidrBlock === '10.0.0.0/16'`
- Test: Subnet normalization — input `{subnets: [{id: '/subscriptions/x/.../subnets/default', addressPrefix: '10.0.1.0/24'}]}`, assert `SubnetId`, `VpcId` (derived from id split on '/subnets/'), `CidrBlock`
- Test: NSG normalization — input `{sgs: [{id: '/subs/x/.../nsg1', name: 'nsg1', securityRules: [{direction: 'Inbound', protocol: 'TCP', destinationPortRange: '443', sourceAddressPrefix: '*'}], defaultSecurityRules: []}]}`, assert `IpPermissions` array is populated with `FromPort: 443`, `ToPort: 443`, `IpProtocol: 'tcp'`
- Test: NIC normalization — input `{enis: [{id: '/subs/x/.../nic1', ipConfigurations: [{subnet: {id: '/subs/x/.../subnets/default'}}]}]}`, assert `NetworkInterfaceId`, `SubnetId`, `VpcId` set
- Test: properties self-reference — after normalization, `vpc.properties === vpc` (so `vpc.properties.addressSpace` resolves)
- Test: empty data object — `_normalizeAzureResources({})` does not throw

**normalization.test.mjs — R1.3: _normTags**
- Test: Azure object tags — input `{tags: {Environment: 'prod', Owner: 'team-a'}}`, assert `r.Tags` is `[{Key: 'Environment', Value: 'prod'}, {Key: 'Owner', Value: 'team-a'}]`
- Test: no tags + has name — input `{name: 'my-vnet'}`, assert `r.Tags` is `[{Key: 'Name', Value: 'my-vnet'}]`
- Test: null input — `_normTags(null)` does not throw
- Test: already has Tags array — input `{Tags: [{Key: 'A', Value: 'B'}]}`, assert Tags not overwritten
- Test: empty tags object — input `{tags: {}}`, assert `r.Tags` is `[]`

**normalization.test.mjs — R1.4: _friendlyFolderLabel**
- Test: named subscription — `_friendlyFolderLabel('azure-export-lmat-PROD-20260330-075242')` returns `'lmat-PROD'`
- Test: GUID subscription — `_friendlyFolderLabel('azure-export-00b800a2-140e-4a89-94f9-ed6aab746a51-20260330-105511')` returns `'00b800a2-140e...'`
- Test: short folder — `_friendlyFolderLabel('my-folder')` returns `'my-folder'`
- Test: long folder — `_friendlyFolderLabel('a-very-long-folder-name-that-exceeds-thirty-characters')` returns truncated string ending with `'...'`
- Test: null — `_friendlyFolderLabel(null)` returns `null`

**normalization.test.mjs — R1.5: matchFile**
- Test: exact filename match — `matchFile('vnets.json', null)` returns `'in_vnets'`
- Test: exact match for subnets — `matchFile('subnets.json', null)` returns `'in_subnets'`
- Test: exact match for nsgs — `matchFile('nsgs.json', null)` returns `'in_nsgs'`
- Test: exact match for nics — `matchFile('nics.json', null)` returns `'in_nics'`
- Test: exact match for public-ips — `matchFile('public-ips.json', null)` returns `'in_pubips'`
- Test: exact match for route-tables — `matchFile('route-tables.json', null)` returns `'in_udrs'`
- Test: content fallback — `matchFile('UNKNOWN.json', '{"addressSpace":{"addressPrefixes":["10.0.0.0/8"]}}')` returns `'in_vnets'`
- Test: completely unknown — `matchFile('random.json', '{"foo":"bar"}')` returns `null`

**utils.test.mjs — R1.2: ext() edge cases (ADD to existing file)**
- Test: flat Azure CLI array fallback — `ext([{id: '/subs/x/vnets/hub', name: 'hub', type: 'Microsoft.Network/virtualNetworks'}], ['value'])` returns the input array as-is (flat array path)
- Test: wrapped object — `ext({value: [{id: '1'}, {id: '2'}]}, ['value'])` returns `[{id:'1'},{id:'2'}]`
- Test: flat array with no Azure markers — `ext([{foo: 'bar'}], ['value'])` returns `[]` (no .id, .name, or .type)
- Test: single resource object (not array) — `ext({securityRules: [{priority: 100}]}, ['securityRules'])` returns `[{priority: 100}]`
  </behavior>
  <action>
**File 1: tests/unit/normalization.test.mjs**

Create a new test file following the existing pattern in utils.test.mjs:
```javascript
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { _normalizeAzureResources, _normTags, matchFile, fileMap, _friendlyFolderLabel } from '../../src/modules/normalization.js';
```

Organize into 4 describe blocks: `_normalizeAzureResources` (R1.1), `_normTags` (R1.3), `_friendlyFolderLabel` (R1.4), `matchFile` (R1.5). Each test in the `<behavior>` block above becomes an `it()` call.

For `_normalizeAzureResources` tests: create a minimal data object with only the array being tested (e.g., `{vpcs: [...]}` for VNet tests). Call `_normalizeAzureResources(d)` then assert properties on the mutated objects.

For `matchFile` tests: note that content parameter is a string (JSON text) not a parsed object. Pass raw JSON strings where content-based detection is being tested.

Also add a test for `fileMap`: `assert.equal(fileMap.length, 70)` to catch accidental additions/deletions.

**File 2: tests/unit/utils.test.mjs**

Add a new describe block at the end of the existing file (after the last existing describe block) for `ext() edge cases (R1.2)`. Do NOT modify any existing tests. Only append.

Add these 4 new tests as specified in the behavior block above.
  </action>
  <verify>
    <automated>cd /Users/schylerryan/Desktop/Github/Azure-Mapper && node --test tests/unit/normalization.test.mjs tests/unit/utils.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/normalization.test.mjs` exits 0
    - `grep -c "describe(" tests/unit/normalization.test.mjs` returns at least 4 (one per function group)
    - `grep -c "it(" tests/unit/normalization.test.mjs` returns at least 18
    - `node --test tests/unit/utils.test.mjs` exits 0, all existing + new tests pass
    - `grep -c "flat Azure CLI" tests/unit/utils.test.mjs` returns at least 1 (new ext test)
  </acceptance_criteria>
  <done>All R1.1-R1.5 unit tests pass. normalization.test.mjs covers _normalizeAzureResources, _normTags, _friendlyFolderLabel, matchFile. utils.test.mjs has new ext() edge case coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test (R1.6), regression test (R1.8), structural check (R1.7), and test script update</name>
  <files>tests/unit/integration.test.mjs, tests/unit/rlctx-regression.test.mjs, package.json</files>
  <read_first>
    - src/modules/normalization.js (for _normalizeAzureResources signature and expected input keys)
    - src/modules/utils.js (for ext() — needed to parse wrapped exports in integration test)
    - azure-export-00b800a2-140e-4a89-94f9-ed6aab746a51-20260330-105511/vnets.json (first 20 lines — understand data format)
    - azure-export-00b800a2-140e-4a89-94f9-ed6aab746a51-20260330-105511/subnets.json (first 20 lines)
    - .planning/phases/02-automated-test-suite/02-RESEARCH.md (code examples section for R1.8 STALE_PATTERNS and _rlCtx key lists)
    - src/modules/unified-dashboard.js (first 60 lines — check exports for R1.7 structural check)
    - src/modules/dashboards.js (first 60 lines — check exports for R1.7 structural check)
    - package.json (current test scripts)
  </read_first>
  <behavior>
**integration.test.mjs — R1.6**
- Test: Load vnets.json from real export folder, parse with ext(), normalize with _normalizeAzureResources, assert VNets have VpcId and CidrBlock set
- Test: Load subnets.json, normalize, assert subnets have SubnetId, VpcId, CidrBlock set
- Test: Load nsgs.json, normalize, assert NSGs have GroupId and IpPermissions
- Test: All normalized resources have Tags array (from _normTags)
- Test: Normalized VNets have properties self-reference (v.properties === v)

**rlctx-regression.test.mjs — R1.8**
- Test: Scan all src/modules/*.js files for stale _rlCtx key patterns (vpcs, sgs, nacls, enis, igws, nats[^G], vpces, instances, albs, rdsInstances, ecsServices, lambdaFns, ecacheClusters, redshiftClusters). Assert zero matches.
- Test: Verify the correct Azure-named keys exist in a reference list (vnets, subnets, nsgs, nics, natGateways, privateEndpoints, vms, appGateways, etc.)

**rlctx-regression.test.mjs — R1.7 (structural/DOM-less check)**
- Test: src/modules/dashboards.js is importable and exports expected functions (verifying module structure without needing a browser)
- Test: src/modules/unified-dashboard.js is importable and exports expected functions
- Note: Full DOM rendering test for R1.7 requires Playwright and is covered in Phase 3 (E2E). This structural check confirms the modules are loadable and export the right interface.
  </behavior>
  <action>
**File 1: tests/unit/integration.test.mjs (R1.6)**

```javascript
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';
import { ext } from '../../src/modules/utils.js';
import { _normalizeAzureResources } from '../../src/modules/normalization.js';
```

Use `import.meta.dirname` to resolve the export folder path relative to repo root:
```javascript
const EXPORT_DIR = join(import.meta.dirname, '../../azure-export-00b800a2-140e-4a89-94f9-ed6aab746a51-20260330-105511');
```

For each test:
1. Read the JSON file with `readFileSync(join(EXPORT_DIR, 'vnets.json'), 'utf8')`
2. Parse with `JSON.parse()`
3. Use `ext(parsed, ['value'])` to handle both wrapped `{value:[...]}` and flat array formats
4. Build the data object: `const d = { vpcs: vnets }` (using the internal key names that `_normalizeAzureResources` expects)
5. Call `_normalizeAzureResources(d)`
6. Assert properties on the normalized resources

Test that at least one VNet has `VpcId` set and non-empty, `CidrBlock` set and non-empty, `Tags` array, and `properties` self-reference.

**File 2: tests/unit/rlctx-regression.test.mjs (R1.8 + R1.7 structural)**

For R1.8: Scan `src/modules/` directory:
```javascript
import { readdirSync, readFileSync } from 'node:fs';
import { join } from 'node:path';
```

Define the 14 stale key patterns as regex array (from RESEARCH.md code examples section). For each `.js` file in `src/modules/`, read its content and test against each pattern. Collect all violations into an array. Assert the violations array is empty, and if not, the assertion error message should list each violation (file + matched pattern + line number).

For R1.7 structural check: Import the dashboard modules and verify they export functions. Use a try/catch — if the import fails because of DOM references (like `document.getElementById`), the test should note this is expected for DOM-dependent modules and still pass for the structural check. The key assertion is that the module files exist and are syntactically valid JavaScript.

Check if dashboards.js and unified-dashboard.js have DOM guards. If they access `document` at top level without guards, the import will fail in Node.js. In that case, use `readFileSync` to verify the file exists and contains expected export patterns via string matching (grep for `export {` or `export function`).

**File 3: package.json update**

The `test:node` script currently runs `node --test tests/unit/*.test.mjs`. This will automatically pick up the new test files since they match the glob. No change needed to `test:node`.

However, verify `test:all` script still works: `jest && node --test tests/unit/*.test.mjs && npx playwright test`. If playwright is not configured for headless in CI, the `test:all` script may fail on Playwright. Leave `test:all` as-is since it already includes the node:test glob.

Only update package.json if a change is actually needed (e.g., if the glob pattern doesn't cover the new files). The current glob `tests/unit/*.test.mjs` already covers all new files.
  </action>
  <verify>
    <automated>cd /Users/schylerryan/Desktop/Github/Azure-Mapper && node --test tests/unit/integration.test.mjs tests/unit/rlctx-regression.test.mjs 2>&1 | tail -5 && echo "---FULL---" && node --test tests/unit/*.test.mjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/integration.test.mjs` exits 0
    - `grep -c "readFileSync" tests/unit/integration.test.mjs` returns at least 2 (loading real export JSON)
    - `grep -c "VpcId" tests/unit/integration.test.mjs` returns at least 1
    - `node --test tests/unit/rlctx-regression.test.mjs` exits 0
    - `grep -c "_rlCtx" tests/unit/rlctx-regression.test.mjs` returns at least 5 (stale key patterns)
    - `node --test tests/unit/*.test.mjs` exits 0 — full unit suite passes (232 existing + all new tests)
    - `npm run test:unit` exits 0 (Jest + node:test combined)
  </acceptance_criteria>
  <done>Integration test loads real Azure export data and verifies normalization pipeline. Regression test catches stale _rlCtx keys. Structural check confirms dashboard modules are valid. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `node --test tests/unit/normalization.test.mjs` — all normalization unit tests pass
2. `node --test tests/unit/integration.test.mjs` — integration test with real data passes
3. `node --test tests/unit/rlctx-regression.test.mjs` — regression + structural tests pass
4. `node --test tests/unit/utils.test.mjs` — all utils tests pass (existing + new ext() tests)
5. `node --test tests/unit/*.test.mjs` — complete suite passes
6. `npm run test:unit` — Jest + node:test combined passes
</verification>

<success_criteria>
- R1.1: _normalizeAzureResources tested for VNets, subnets, NSGs, NICs, properties self-ref
- R1.2: ext() tested for flat arrays, wrapped objects, and edge cases
- R1.3: _normTags tested for Azure objects, name fallback, null, empty
- R1.4: _friendlyFolderLabel tested for named subs, GUIDs, short/long folders, null
- R1.5: matchFile tested for exact matches, content fallback, unknown files
- R1.6: Integration test loads real export folder, normalizes, verifies key properties
- R1.7: Structural check confirms dashboard modules exist and export functions (full DOM test deferred to Phase 3 Playwright)
- R1.8: Regression test scans all module files for stale _rlCtx keys, asserts zero matches
- All tests run under `node --test tests/unit/*.test.mjs` with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-automated-test-suite/02-02-SUMMARY.md`
</output>
